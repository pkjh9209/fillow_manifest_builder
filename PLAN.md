# Fillow 매니페스트 빌더 — 기획서

> 결과물: `fillow_manifest_builder/index.html` (단일 파일)
> 대상 스펙: `fillow_diary/docs/asset-manifest.md`, `scripts/build-inventory.mjs`, `src/game/mapConfig.js`
> 작성일: 2026-08-19

---

## 0. 확정된 방향

| 항목 | 결정 |
|---|---|
| 앱 형태 | **단일 HTML 파일** (CSS/JS 인라인, 빌드·설치·의존성 0) |
| 저장 방식 | **폴더 직접 저장**(File System Access API) + 다운로드 폴백 |
| 1차 기능 | 기본 키 폼 + **footprint 픽셀 페인팅** + tint 팔레트 에디터 |
| 검증 | `build-inventory.mjs` 경고 규칙 그대로 재현 |
| 부가 | footprint 시뮬레이션 미리보기 |
| **배율** | `TEST_ASSET_SCALE.multiplier` = **1** (정수 고정). `mapConfig.js`도 함께 수정 완료 |
| **2차로 미룸** | `rotation` (n/e/s/w 방향별 이미지) — 12장 참고 |
| **제외** | `id` 중복 검사 / `kind`의 `floor` 값 (`wall`·`cap`·`tile` 3종만) |

`etc` 카테고리는 `public/asset` 아래 폴더가 아직 없지만 select에 그대로 둔다.

### 상태: 구현 완료 (M1~M4)

`index.html` 하나로 전부 들어갔다. 자가 테스트 21개 통과 (`index.html?test=1`).

---

## 1. 왜 만드는가

지금은 `.json`을 손으로 쓴다. 다른 키는 타이핑이면 끝나지만 **`footprint`만은 눈으로 세야 한다.**

배율이 `1.3`이던 시절에는 한 칸이 원본 `16 / 1.3 ≈ 12.31px`라는 **소수 격자**여서 아예 셀 수가 없었다.
그래서 현행 문서조차 "직사각형 오브젝트는 그냥 생략하는 편이 안전하다"고 **포기를 권고**하고 있었고,
결과적으로 ㄱ자 책상·L자 소파 같은 비정형 가구가 실제로는 빈 칸까지 막아버린다.

**배율을 1로 정리하면서 한 칸 = 원본 16px 정사각형이 되었다.** 이제 세는 것 자체는 가능하지만,
16의 배수가 아닌 에셋(46×46, 52×36 …)은 여전히 반올림 때문에 격자와 그림이 어긋나고,
비정형 오브젝트의 칸을 하나씩 적어 내려가는 일은 손으로 할 작업이 아니다.

**이 빌더의 존재 이유는 그 격자를 그림 위에 띄워놓고 마우스로 칠하게 만드는 것이다.**
나머지 폼·검증·저장은 그 김에 얹는 편의 기능이다.

---

## 2. 격자 계산 규칙 ★ 가장 중요 ★

`RoomScene.getItemFootprint()` / `getAbsoluteCells()`를 **한 글자도 다르지 않게** 재현해야 한다.
여기가 틀리면 빌더가 만든 footprint가 게임에서 어긋난다.

```js
const TILE       = 16;    // mapConfig.TILE
const MULTIPLIER = 1;     // mapConfig.TEST_ASSET_SCALE.multiplier — 정수 고정

// 화면 표시 크기
const displayW = imgW * MULTIPLIER;
const displayH = imgH * MULTIPLIER;

// 격자 칸 수  ← Math.round 고정. ceil/floor 금지
const cols = Math.max(1, Math.round(displayW / TILE));
const rows = Math.max(1, Math.round(displayH / TILE));

// 원본 이미지 픽셀 기준 한 칸의 크기
const cellSrcPx = TILE / MULTIPLIER;   // = 16 (배율 1일 때 원본 16px 정사각형)
```

- **원점**: 이미지 바운딩박스의 **좌상단**이 `[0,0]`. (`getAbsoluteCells`가 `gx = round((x - displayWidth/2) / TILE)`로 좌상단 칸을 잡는다)
- **좌표**: `[dx, dy]` = `[열, 행]`. `x`가 먼저.
- **`Math.round`는 반올림 위치가 JS 기준**(`.5`는 위로). Python `round()`처럼 은행가 반올림을 쓰면 값이 어긋나므로 구현 시 반드시 `Math.round` 사용.

### 격자와 이미지가 안 맞는다 (버그가 아니라 사양)

`round` 때문에 격자 전체 크기와 이미지 크기는 **일치하지 않는다.** 양방향 모두 발생한다.

실제 에셋 기준 (배율 1):

| 에셋 | 이미지 | 격자 | 격자가 덮는 영역 | 관계 |
|---|---|---|---|---|
| `wood_tile` | 32 × 32 | **2 × 2** | 32 × 32 | 딱 맞음 |
| `cap_top_brown_01` | 32 × 16 | **2 × 1** | 32 × 16 | 딱 맞음 |
| `default_desk` | 46 × 46 | **3 × 3** | 48 × 48 | 격자가 **+2px 큼** |
| `default_carpet` | 52 × 36 | **3 × 2** | 48 × 32 | 격자가 **−4px 작음** (그림 오른쪽·아래가 안 잡힘) |
| `default_sofa` | 54 × 39 | **3 × 2** | 48 × 32 | 격자가 −6, −7px 작음 |
| `wall_normal_white` | 32 × 92 | **2 × 6** | 32 × 96 | 세로만 +4px 큼 |
| `default_bed` | 32 × 69 | **2 × 4** | 32 × 64 | 세로 5px 잘림 |

→ **UI 요구사항**: 이미지 테두리(흰 실선)와 격자 바깥 경계(노란 점선)를 **다른 색으로 같이** 그려서,
"이 칸은 그림 밖까지 먹는다 / 이 그림 끄트머리는 격자에 안 잡힌다"를 눈으로 보이게 한다.

### 상수는 한 곳에 모은다

`TILE`과 `MULTIPLIER`는 `mapConfig.js`에서 오는 값이다. 다이어리 쪽이 바뀌면 빌더도 바뀌어야 한다.

- 파일 최상단 `const CONFIG = { TILE: 16, MULTIPLIER: 1 }` 한 군데에만 둔다.
- **UI 상단에도 두 값을 입력창으로 노출**한다 (기본값 16 / 1). 다이어리에서 배율을 만졌을 때
  빌더 코드를 안 고치고도 맞출 수 있고, 무엇보다 "이 격자는 이 두 숫자에서 나온다"가 화면에 보인다.
- **배율 입력창은 정수만 받는다** — 소수를 넣으면 즉시 반올림해서 되돌린다.
- 값 옆에 `격자 3 × 3칸 · 한 칸 = 원본 16.00px`을 실시간으로 표시.

---

## 3. 화면 구성

```
┌───────────────────────────────────────────────────────────────────────────┐
│  Fillow 매니페스트 빌더          TILE [16]  배율 [1.3]   격자 4×4 (12.31px) │
├──────────────────────────────────────────┬────────────────────────────────┤
│                                          │  ■ 기본 정보                    │
│   [도구] 브러시 지우개 사각형 전체 비우기 │   id       [furniture-desk-…]  │
│   [보기] 격자☑ 이미지☑ 칠☑  줌 [1x▸8x]   │   name     [                ]  │
│                                          │   category [furniture ▾]       │
│    ┌─────────────────────────────┐       │   kind     (map일 때만 표시)    │
│    │                             │       │   qty      [1]                 │
│    │      이미지 + 격자 캔버스     │       │   desc     [                ]  │
│    │      (칠한 칸 = 청록 반투명)  │       │   imgW/H   46 / 46  (자동)     │
│    │                             │       │   frameW/H [  ] / [  ] (옵션)  │
│    └─────────────────────────────┘       │                                │
│                                          │  ■ footprint                   │
│   자동 추정: 알파 임계 [10%] [적용]       │   12칸 / 16칸 선택됨            │
│   ↶ 되돌리기  ↷ 다시                     │   ☐ 사각형이어도 명시해서 출력   │
│                                          │                                │
├──────────────────────────────────────────┤  ■ tint 팔레트   [+ 색 추가]    │
│  ■ 시뮬레이션 미리보기                    │   ● natural 원목  #ffffff  ⦿기본│
│    (방 격자 위에 얹은 모습 / 충돌칸 빨강) │   ● walnut  월넛  #a9713f  ○   │
├──────────────────────────────────────────┴────────────────────────────────┤
│  ■ 출력 미리보기 (JSON)                    ■ 검증                          │
│   { "id": "furniture-desk-…",              ⚠ tint.palette[1] color 형식…  │
│     "footprint": [[0,0],[1,0]] … }         ✖ id가 비어 있음                │
│                                                                           │
│   파일명 [default_desk] .json / .png    [폴더 지정해 저장]  [다운로드]      │
└───────────────────────────────────────────────────────────────────────────┘
```

**레이아웃**: 좌측 = 캔버스 작업 영역, 우측 = 폼, 하단 전폭 = 출력/검증.
브라우저 폭 1200px 미만이면 세로 1단으로 접힘.

---

## 4. 캔버스 & 페인팅

### 렌더 레이어 (아래 → 위)

1. 투명 배경 체커보드 (밝은 회색 격자무늬)
2. 이미지 — `imageSmoothingEnabled = false` (도트 유지, 정수 배율만 허용)
3. 격자선 — 얇은 흰선, `cellSrcPx × zoom` 간격
4. 칠한 칸 — `rgba(0,200,200,0.35)` + 칸 테두리
5. 이미지 경계(흰 실선) / 격자 경계(노란 점선) — 2장 참고
6. 마우스 커서 아래 칸 하이라이트 + 좌표 툴팁 `[2,1]`

### 줌

`1x / 2x / 4x / 8x / 16x` **정수 배율만**. 소수 배율은 도트가 반 픽셀에 걸쳐 뭉개진다
(`mapConfig.js`의 `WORLD_SCALE` 주석과 같은 이유). 기본은 이미지가 화면에 400px 정도로 보이는 배율 자동 선택.

### 도구

| 도구 | 동작 |
|---|---|
| 브러시 | 드래그하는 동안 지나간 칸을 칠함. 시작 칸이 이미 칠해져 있으면 **드래그 내내 지우기**로 동작 (토글 브러시) |
| 지우개 | 드래그로 지움 |
| 사각형 | 드래그한 사각 범위를 한 번에 채움 / `Alt`+드래그면 비움 |
| 전체 채움 | `cols × rows` 전부 칠함 (= footprint 생략과 동일한 상태) |
| 전체 비움 | 전부 지움 |
| 자동 추정 | 이미지 알파를 읽어, 칸 안에서 `alpha > 0`인 픽셀 비율이 **임계값(기본 10%) 이상**이면 칠함. 슬라이더로 조절하며 즉시 미리보기 |

- **Undo / Redo**: `Ctrl+Z` / `Ctrl+Shift+Z`. 드래그 한 번 = 히스토리 1개 (드래그 시작 시 스냅샷).
- **키보드**: `B` 브러시, `E` 지우개, `R` 사각형, `A` 자동추정, `G` 격자 토글.

### 자동 추정은 "출발점"이지 정답이 아니다

알파 기준 추정은 그림자·러그처럼 **밟고 지나가야 하는** 부분까지 칠해버린다.
자동 추정 결과는 항상 **수동 수정 전제**로 취급하고, 적용 직후 안내를 띄운다:
"그림자·카펫처럼 지나갈 수 있어야 하는 칸은 지워주세요."

---

## 5. 기본 키 폼

| 필드 | 필수 | 입력 방식 | 검증·동작 |
|---|:---:|---|---|
| `id` | ✅ | 텍스트 + [자동 생성] | 공백·대문자 경고. 자동 생성 = `<category>-<폴더명>-<파일명>` 케밥케이스 |
| `name` | ✅ | 텍스트 | 비면 에러 |
| `category` | ✅ | select: `furniture` / `deco` / `etc` / `map` | `mockInventory.js`의 4종 고정 |
| `kind` | map만 | select: `wall` / `cap` / `tile` | `category === 'map'`일 때만 나타나고 그때 필수 |
| `qty` | ❌ | number, 기본 1 | 2 이상일 때만 카드에 뱃지가 뜬다고 힌트 표시 |
| `desc` | ❌ | 텍스트 | 카드 툴팁으로 쓰인다고 힌트 |
| `imgW` / `imgH` | ✅ | **업로드 이미지에서 자동 추출**, 읽기 전용 | [수동 입력] 토글로 잠금 해제. 수동 값이 실제 이미지와 다르면 경고 |
| `frameW` / `frameH` | ❌ | number 쌍 | 스프라이트시트(캐릭터/펫)용. 하나만 채우면 경고 |

- **`image` 키는 절대 출력하지 않는다.** (`build-inventory.mjs`가 파일 위치로 계산)
- 폼 변경 → 출력 미리보기·검증 즉시 갱신 (디바운스 없이).

---

## 6. tint 팔레트 에디터

```json
"tint": { "default": "natural", "palette": [ { "id": "natural", "name": "원목", "color": "#ffffff" } ] }
```

- 행 = `[≡드래그핸들] [id] [name] [🎨색상피커] [#hex] [⦿기본] [🗑]`
- **곱연산 미리보기**: 캔버스에 `globalCompositeOperation = 'multiply'`로 색을 얹어 실제 Phaser `setTint` 결과와 같은 그림을 보여준다. 팔레트 행에 마우스를 올리면 그 색으로 캔버스가 바뀜.
- 팔레트에 `#ffffff`가 없으면 안내: **"원본 색 그대로를 고를 수 없습니다. `#ffffff` 항목을 넣으세요."**
- 원본 에셋의 평균 밝기를 계산해서 어두우면 힌트: "원본이 어두워 틴트가 탁해집니다. 밝은 회색조 에셋에서 색이 예쁘게 입습니다."
- `tint`를 안 쓰면 **키 자체가 출력되지 않아야 한다** (빈 `tint: {}` 금지). 섹션 상단에 on/off 체크박스.

---

## 7. 검증 패널 — `build-inventory.mjs` 규칙 재현

빌드 로그에서 뒤늦게 발견하는 대신 **저장 전에** 같은 문구로 보여준다.
`✖ 에러`는 저장 버튼을 막고, `⚠ 경고`는 막지 않는다.

### build-inventory와 1:1 대응

| 레벨 | 조건 | 메시지 |
|:---:|---|---|
| ✖ | 이미지 미업로드 | 짝이 되는 이미지를 못 찾아 건너뜀 |
| ⚠ | `palette[i].id` 없음 | `tint.palette[i]`에 id가 없어 건너뜀 |
| ⚠ | `color`가 `/^#[0-9a-fA-F]{6}$/` 불일치 | `tint.palette[i](id)`의 color가 "#RRGGBB" 형식이 아니라 건너뜀 |
| ⚠ | `palette` id 중복 | `tint.palette`에 중복된 id가 있어 뒤엣것을 건너뜀 |
| ⚠ | 유효 항목 0개 | 쓸 수 있는 `tint.palette` 항목이 없어 틴트 설정을 무시함 |
| ⚠ | `default`가 palette에 없음 | `tint.default "x"`가 palette에 없어 "첫항목"으로 대체함 |

### 빌더 자체 검증 (추가)

| 레벨 | 조건 |
|:---:|---|
| ✖ | `id` 비었음 / `name` 비었음 |
| ✖ | `category`가 4종 밖 |
| ✖ | `category === 'map'`인데 `kind` 없음 |
| ✖ | `imgW`/`imgH`가 0 또는 비어 있음 |
| ⚠ | `id`에 공백·대문자·특수문자 |
| ⚠ | footprint 칸이 **0개** (칠한 게 없음) → 게임에선 이미지 전체 사각형으로 취급됨 |
| ⚠ | footprint 좌표가 `0..cols-1 / 0..rows-1` 밖 |
| ⚠ | `frameW`/`frameH` 중 하나만 채움 |
| ⚠ | `qty`가 0 이하 또는 정수 아님 |

---

## 8. 시뮬레이션 미리보기

캔버스 아래 접이식 패널.

- 배경에 `TILE × MULTIPLIER` 간격 격자를 깔고(= 게임 안 격자와 같은 실척),
  이미지를 `MULTIPLIER` 배로 얹은 뒤 **칠한 칸을 빨강 반투명**으로 겹쳐 그린다.
- 즉 "게임에서 실제로 이 그림이 이 칸을 막는다"를 그대로 보여준다.
- 참고선 옵션: 캐릭터 1인분 크기 사각형 / 방 벽 경계(`WALL_ROWS = 6`).
- 이 패널만으로 격자 계산이 맞는지 눈으로 검증된다 — 2장의 소수 격자 문제를 사람이 확인하는 유일한 창구.

---

## 9. 출력 & 저장

### JSON 직렬화 규칙

키 순서는 기존 예시 파일과 동일하게 고정한다:

```
id → name → category → kind → qty → desc → imgW → imgH → footprint → frameW → frameH → tint
```

- **비어 있는 옵션 키는 아예 출력하지 않는다** (`null`/`""`/빈 배열/빈 객체 금지).
- **footprint 생략 규칙**: 칠한 칸이 `cols × rows` **꽉 찬 사각형과 정확히 일치**하면
  `footprint` 키를 **뺀다**. 게임이 어차피 같은 값을 기본값으로 계산하고,
  문서도 "직사각형은 생략하는 편이 안전하다"고 권한다.
  → 굳이 남기고 싶으면 `☐ 사각형이어도 명시해서 출력` 체크박스로 강제.
- footprint 배열 서식: `[[0,0],[1,0],[0,1]]` **한 줄**. 칸이 12개를 넘으면 행 단위로 줄바꿈해서 모양이 눈에 보이게:
  ```json
  "footprint": [
    [0,0],[1,0],[2,0],
    [0,1],        [2,1]
  ]
  ```
  (정렬 공백은 JSON 유효 — 사람이 읽기 위한 배려)
- 들여쓰기 2칸, 파일 끝 개행 1개.

### 파일명

회전이 없는 에셋은 **이미지와 매니페스트 파일명이 같아야** `build-inventory`가 짝을 찾는다.
따라서 파일명 입력은 하나뿐이고, 거기에 `.png` / `.json`을 각각 붙여 두 파일을 만든다.

### 캐시 규칙 가드 ★

`vercel.json`이 `/asset/**`에 1년 immutable 캐시를 건다. **같은 파일명으로 그림만 바꾸면
기존 유저에게 1년간 반영되지 않는다.**

- 저장하려는 폴더에 **같은 파일명이 이미 있으면** 저장을 막고 경고:
  > 이 파일명은 이미 있습니다. 덮어쓰면 캐시 때문에 기존 유저에게 1년간 반영되지 않습니다.
  > `default_desk_v2` 로 바꿔 저장하세요. **`id`는 절대 바꾸지 마세요** — 유저 방에 놓인 가구가 사라집니다.
- `[_v2 붙이기]` 버튼 제공 (이미 `_vN`이면 `N+1`).
- 그때 `id` 필드에는 **잠금 아이콘**을 띄우고 "교체 시 id 유지" 툴팁.

### 저장 흐름

1. `[폴더 지정해 저장]` → `showDirectoryPicker()`
2. 선택한 폴더가 `public/asset/<category>/…` 경로 안인지 이름으로 추정해 안내 (강제하진 않음)
3. 중복 파일명 검사 → 위 가드
4. `<name>.png` + `<name>.json` 두 파일을 **같은 폴더에** 쓴다
5. 성공 시: `npm run build:inventory` 를 실행하라는 안내 + 명령어 복사 버튼

**폴백**: File System Access API 미지원(Firefox/Safari)이거나 사용자가 취소하면
`[다운로드]`로 두 파일을 각각 내려받기. 이때는
"두 파일을 **같은 폴더에, 같은 이름으로** 넣으세요" 안내를 크게 띄운다.

---

## 10. 내부 상태 모델

```js
const state = {
  config: { TILE: 16, MULTIPLIER: 1.3 },

  image: { fileName, blob, bitmap, w, h, alphaMap },   // alphaMap = 자동추정용 캐시

  grid:  { cols, rows, cellSrcPx },                    // config + image에서 파생 (계산 전용)

  cells: new Set(),                                    // "dx,dy" 문자열. footprint의 원천

  form:  { id, name, category, kind, qty, desc,
           imgW, imgH, imgSizeManual, frameW, frameH },

  tint:  { enabled, default: 'natural',
           palette: [{ id, name, color }] },

  view:  { zoom, showGrid, showImage, showCells, tool, alphaThreshold },

  history: { past: [], future: [] },                   // cells 스냅샷만
};
```

**단방향 흐름**: `state 변경 → render()` 하나로 캔버스·미리보기·검증을 전부 다시 그린다.
크기가 작아서 diff 최적화는 불필요하고, 상태가 어긋날 여지가 없다.

---

## 11. 구현 단계

| 단계 | 내용 | 이 단계가 끝나면 |
|---|---|---|
| **M1** | 이미지 업로드 → 격자 계산 → 칸 칠하기 → footprint 배열 화면 출력 | **핵심 가치 완성.** 이것만으로도 손계산이 사라짐 |
| **M2** | 기본 키 폼 + 검증 패널 + JSON 미리보기 | 온전한 매니페스트가 화면에 나옴 |
| **M3** | tint 에디터 + 곱연산 미리보기 | 색상 에셋 대응 |
| **M4** | 폴더 저장 / 다운로드 + 캐시 가드 + 시뮬레이션 패널 | 배포 가능 |
| **M5** | (2차) `rotation` | 12장 |

각 단계마다 `fillow_manifest_builder/index.html` 하나만 갱신된다.

### 코드 구조 (단일 파일 안의 섹션 순서)

```
<style>  ... 전체 CSS
<body>   ... 정적 마크업 (템플릿 엔진 없음)
<script>
  1. CONFIG / 상수
  2. state
  3. 이미지 로더 (파일 선택 · 드래그앤드롭 · 붙여넣기)
  4. 격자 계산  ← 2장 공식. 여기만 단위 테스트 대상
  5. 캔버스 렌더
  6. 포인터 입력 (브러시/사각형/줌/팬)
  7. 자동 추정 (알파 스캔)
  8. 폼 바인딩
  9. tint 에디터
 10. 검증기       ← 7장 표를 그대로 함수화
 11. 직렬화       ← 9장 규칙
 12. 저장 (FS API / 다운로드)
 13. 시뮬레이션 렌더
```

4번과 10, 11번은 부수효과 없는 **순수 함수**로 두고, 파일 하단에 간단한 자가 테스트
(`?test=1` 쿼리로 실행)를 붙인다. 2장 표의 수치가 그대로 테스트 케이스가 된다.

---

## 12. 2차 확장 — `rotation`

1차에서 뺀 이유: 방향별로 **이미지·`imgW/imgH`·`footprint`가 각각 따로**라
사실상 지금 만드는 편집기가 4벌 필요하다. 구현량이 나머지 전부와 비슷하다.

넣을 때의 설계:

- 상단에 `[ n ][ e ][ s ][ w ][+]` 탭. 쓰는 방향만 추가 (좌우 2방향 소파는 `e`/`w`만).
- 탭 전환 시 캔버스가 그 방향의 이미지·격자·칠 상태로 통째로 바뀐다.
- 방향별 `imgW/imgH`는 그 방향 이미지에서 자동, `footprint`는 그 방향에서 따로 칠함.
  **비우면 최상위 값을 물려받는다** — "상속 중"임을 회색 글씨로 표시.
- `default` 방향 라디오. 대표 이미지(썸네일)는 default 방향 파일이 된다.
- `variants[].file`은 **매니페스트 폴더 기준 상대 경로**. 하위 폴더(`sub/x.png`)도 허용.
- 회전 에셋은 매니페스트 파일명이 이미지와 달라도 되므로, 파일명 입력이 **대표 이름 1개 + 방향별 파일명 4개**로 분리된다.
- 시계방향 `n → e → s → w` 순환 미리보기 버튼 — 없는 방향은 건너뛴다.
- 에셋 교체 시엔 **매니페스트 파일명은 그대로 두고 `variants[].file`만 새 파일로** 바꾼다 (9장 캐시 가드의 회전 버전).

---

## 13. 결정 기록

| 항목 | 결정 | 비고 |
|---|---|---|
| `etc` 카테고리 | 그대로 유지 | `public/asset/etc/` 폴더는 아직 없지만 select에 남김 |
| `kind`의 `floor` | 안 씀 | `wall` / `cap` / `tile` 3종만. `MAP_KIND_TO_LAYER`가 `floor`도 받지만 문서 기준을 따름 |
| `id` 중복 검사 | 제외 | 2차 검토 |
| `TEST_ASSET_SCALE.multiplier` | **1.3 → 1** | 테스트용 값이었음. 정수로 정리 |

### 배율 변경 (1.3 → 1) 적용 내역

바꾸기 좋은 타이밍이었다 — **기존 매니페스트 14개 중 `footprint`를 명시한 것이 하나도 없어서,
배율 변경으로 깨질 데이터가 0건이다.** (전부 이미지 크기 그대로의 기본 사각형을 쓰고 있었다)

수정한 파일:

| 파일 | 내용 |
|---|---|
| `src/game/mapConfig.js` | `multiplier: 1.3 → 1`. 주석에 "정수만 쓸 것"과 그 이유(도트 뭉갬 + 소수 격자) 추가 |
| `docs/asset-manifest.md` | "footprint 주의" 절을 배율 1 기준으로 재작성. `Math.round` 공식과 반올림 어긋남 예시 추가, 빌더 사용 안내 추가 |

화면이 이전보다 23% 작아진다. 키우고 싶으면 `mapConfig.js`가 권하는 대로
`WORLD_SCALE`(화면 전체 줌)이나 `BOARD_COLS`/`BOARD_ROWS`(방 칸 수)로 조절한다.
